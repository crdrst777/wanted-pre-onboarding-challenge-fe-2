/**
 * 이름과 나이를 출력해주는 함수.
 *
 * @param {string} name
 * @param {number} age
 * @returns {string} name + age
 */
function hello(name, age) {
  return `name : ${name} , age : ${age}`;
}

hello("picpal", 10);
